using UnityEngine;

public class EnemyDamage : MonoBehaviour
{
    public int DamageValue = 0;
    void Start()
    {
        
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
